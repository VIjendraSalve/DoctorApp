package com.wht.rishiherherbocare.Constant;

/**
 * @author Pratik Butani on 23/4/16.
 */
public interface ApiService {

    /*@Multipart
    @POST(IUrls.URL_USER_REGISTRATION)
    Call<ResponseBody> POSTUpdateProfile(@Part("user_reg_id") RequestBody user_reg_id,
                                         @Part("apiToken") RequestBody apiToken,
                                         @Part MultipartBody.Part file);*/



/*    @Multipart
    @POST(IUrls.URL_UPDATE_PROFILE)
    Call<ResponseBody> POSTUpdateProfile(@Part("user_id") RequestBody user_id,
                                         @Part("first_name") RequestBody first_name,
                                         @Part("last_name") RequestBody last_name,
                                         @Part("email") RequestBody email,
                                         @Part MultipartBody.Part file);


    @Multipart
    @POST(IUrls.URL_ADD_REVIEW)
    Call<ResponseBody> PostOrderReview(
            @Part("user_id") RequestBody user_id,
            @Part("order_id") RequestBody order_id,
            @Part("rating") RequestBody rating,
            @Part("review") RequestBody review,
            @Part MultipartBody.Part review_img);*/



/*










    //@Part MultipartBody.Part[] multiplefiles

    /*@Part List<MultipartBody.Part> RoomPartList,
    @Part @Nullable MultipartBody.Part file*/











   /*



    */
//


  /*  @Multipart
    @POST("smart_steps/update_IDcard.php")
    Call<ResponseBody> getUpdateStationaryStudentIDCardAndProgressCardForm(
            @Part("user_id") RequestBody user_id,
            @Part("stationary_type_id") RequestBody stationary_type_id,
            @Part("ip_id") RequestBody ip_id,
            @Part("student_name") RequestBody student_name,
            @Part("student_group") RequestBody student_group,
            @Part("student_rollno") RequestBody student_rollno,
            @Part("student_dob") RequestBody student_dob,
            @Part("gender") RequestBody gender,
            @Part("academic_year") RequestBody academic_year,
            @Part("pincode") RequestBody pincode,
            @Part("address") RequestBody address,
            @Part("parent_name") RequestBody parent_name,
            @Part("parent_contact_no") RequestBody parent_contact_no,
            @Part MultipartBody.Part file


    );

    @Multipart
    @POST("smart_steps/update_IDcard.php")
    Call<ResponseBody> getUpdateWithoutImageStationaryStudentIDCardAndProgressCardForm(
            @Part("user_id") RequestBody user_id,
            @Part("stationary_type_id") RequestBody stationary_type_id,
            @Part("ip_id") RequestBody ip_id,
            @Part("student_name") RequestBody student_name,
            @Part("student_group") RequestBody student_group,
            @Part("student_rollno") RequestBody student_rollno,
            @Part("student_dob") RequestBody student_dob,
            @Part("gender") RequestBody gender,
            @Part("academic_year") RequestBody academic_year,
            @Part("pincode") RequestBody pincode,
            @Part("address") RequestBody address,
            @Part("parent_name") RequestBody parent_name,
            @Part("parent_contact_no") RequestBody parent_contact_no


    );*/


}
